services:
  flask-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: book_recommendation_system
    container_name: book_recommendation_api
    ports:
      - "5000:5000"
    volumes:
      - .:/app
